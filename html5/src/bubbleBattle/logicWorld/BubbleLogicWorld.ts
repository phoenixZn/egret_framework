class BubbleLogicWorld extends LogicWorld
{
    public constructor()
    {
        super();
    }

    public Init(worldcontext : WorldCreationContext)
	{
        super.Init(worldcontext);
	}

    public async Prepare()
	{
        await super.Prepare();
	}

	public Start()
    {
        super.Start();
    }
}